# 👥 راهنمای مدیریت کاربران

## ✨ قابلیت‌های جدید اضافه شده

### 1️⃣ مدیریت کاربران (مدیران و کارمندان)
پنل ادمین حالا امکان مدیریت کامل کاربران را دارد.

---

## 🎯 دسترسی به صفحه مدیریت کاربران

### از داشبورد:
1. وارد سیستم شوید (http://localhost:3002)
2. روی کارت **"مدیریت کاربران"** کلیک کنید

### از Sidebar:
- در سمت راست صفحه، Sidebar را خواهید دید
- روی **"مدیریت کاربران"** کلیک کنید

---

## 📋 امکانات صفحه مدیریت کاربران

### ✅ مشاهده لیست کاربران
- جدول کامل با ستون‌های:
  - نام و ایمیل
  - شماره موبایل
  - نقش (مدیر / کارمند)
  - بخش
  - تاریخ ایجاد
  - عملیات (ویرایش / حذف)

### 🔍 جستجو و فیلتر
- جستجو بر اساس نام یا شماره موبایل
- فیلتر بر اساس نقش (مدیر / کارمند)
- فیلتر بر اساس بخش

### ➕ ایجاد کاربر جدید
دکمه **"کاربر جدید"** را بزنید و فرم زیر را پر کنید:

#### فیلدهای فرم:
- **نام:** نام کامل کاربر (الزامی)
- **شماره موبایل:** به فرمت 09xxxxxxxxx (الزامی، یونیک)
- **ایمیل:** آدرس ایمیل (اختیاری)
- **نقش:**
  - کارمند (EMPLOYEE)
  - مدیر (MANAGER) - فقط ADMIN می‌تواند انتخاب کند
- **بخش:** انتخاب از لیست بخش‌ها (الزامی)
- **رمز عبور:** حداقل 6 کاراکتر (الزامی)

💡 **نکته:** بعد از ایجاد کاربر، رمز عبور نمایش داده می‌شود. حتماً آن را یادداشت کنید!

### ✏️ ویرایش کاربر
- روی آیکون مداد کلیک کنید
- همه فیلدها را می‌توانید ویرایش کنید
- رمز عبور اختیاری است (خالی بگذارید اگر نمی‌خواهید تغییر دهید)

### 🗑️ حذف کاربر
- فقط **ADMIN** می‌تواند کاربر را حذف کند
- روی آیکون سطل زباله کلیک کنید
- تایید نهایی درخواست می‌شود

---

## 🔐 سطوح دسترسی

### ADMIN (مدیرعامل):
✅ مشاهده همه کاربران (تمام بخش‌ها)
✅ ایجاد MANAGER یا EMPLOYEE
✅ ویرایش همه کاربران
✅ حذف کاربران
✅ انتقال کاربران بین بخش‌ها
✅ تغییر نقش کاربران

### MANAGER (مدیر بخش):
✅ مشاهده کاربران بخش خودش
✅ ایجاد EMPLOYEE فقط در بخش خودش
✅ ویرایش کارمندان بخش خودش
❌ تغییر نقش کاربران
❌ انتقال کاربران به بخش دیگر
❌ حذف کاربران

### EMPLOYEE (کارمند):
❌ دسترسی به صفحه مدیریت کاربران

---

## 🎨 Sidebar نویگیشن

Sidebar جدید در سمت راست صفحه اضافه شده که شامل:

### منوی اصلی (برای همه):
- 🏠 داشبورد
- ✍️ ثبت فیدبک
- 💬 فیدبک‌ها
- ✅ تسک‌ها
- 🔔 اعلانات
- 🏆 بورد افتخارات

### منوی مدیریتی (ADMIN و MANAGER):
- 👥 **مدیریت کاربران** ⭐ جدید
- 🏢 مدیریت بخش‌ها
- 📊 آمار و گزارش‌ها

### اطلاعات کاربر:
- نام و شماره موبایل
- نقش (مدیرعامل / مدیر / کارمند)
- دکمه خروج

### نسخه موبایل:
- دکمه منو در گوشه بالا سمت راست
- Sidebar به صورت overlay نمایش داده می‌شود
- کلیک روی پس‌زمینه تاریک برای بستن

---

## 🚀 نحوه استفاده (مثال عملی)

### مثال: ایجاد یک مدیر جدید برای بخش IT

1. با حساب ADMIN وارد شوید
2. به صفحه **مدیریت کاربران** بروید
3. روی **"کاربر جدید"** کلیک کنید
4. فرم را پر کنید:
   ```
   نام: محمد رضایی
   موبایل: 09121234567
   ایمیل: mohammad@company.com
   نقش: مدیر
   بخش: IT
   رمز عبور: pass123456
   ```
5. روی **"ایجاد کاربر"** کلیک کنید
6. رمز عبور نمایش داده می‌شود - یادداشت کنید!
7. حالا محمد رضایی می‌تواند با موبایل و رمز عبورش وارد شود

### مثال: ویرایش کارمند

1. در لیست کاربران، کارمند مورد نظر را پیدا کنید
2. روی آیکون **مداد** کلیک کنید
3. فیلدهای مورد نظر را ویرایش کنید
4. روی **"بروزرسانی"** کلیک کنید

---

## 📱 API Endpoints

### لیست کاربران
```
GET /api/users
Query params:
  - role: MANAGER | EMPLOYEE
  - departmentId: string
  - search: string
```

### ایجاد کاربر
```
POST /api/users
Body: {
  mobile, name, email, role, departmentId, password
}
```

### مشاهده کاربر
```
GET /api/users/[id]
```

### ویرایش کاربر
```
PATCH /api/users/[id]
Body: {
  mobile?, name?, email?, role?, departmentId?, password?
}
```

### حذف کاربر
```
DELETE /api/users/[id]
```

---

## 🔒 امنیت

- ✅ رمز عبور با bcrypt hash می‌شود (10 rounds)
- ✅ شماره موبایل یونیک است
- ✅ فرمت موبایل چک می‌شود (09xxxxxxxxx)
- ✅ Permission checks در سمت سرور
- ✅ Session-based authentication
- ✅ Role-based access control

---

## 💡 نکات مهم

1. **رمز عبور اولیه:** بعد از ایجاد کاربر، حتماً رمز را ذخیره کنید
2. **شماره موبایل یونیک:** هر شماره فقط یک بار قابل ثبت است
3. **حذف محدود:** فقط ADMIN می‌تواند کاربر حذف کند
4. **نمی‌توانید خودتان را حذف کنید**
5. **ADMIN ها قابل حذف نیستند**

---

## 🎉 موفق باشید!

حالا می‌توانید تمام مدیران و کارمندان خود را از طریق پنل مدیریت کنید!
