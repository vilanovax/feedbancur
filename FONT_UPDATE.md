# تغییر فونت به وزیر متن

## تغییرات انجام شده

فونت اپلیکیشن از Inter (فونت انگلیسی) به **Vazirmatn** (فونت فارسی) تغییر یافت.

### ✅ فایل‌های بروزرسانی شده:

1. **app/layout.tsx**
   - فونت Inter حذف شد
   - فونت Vazirmatn با 4 وزن (400، 500، 600، 700) اضافه شد
   - از localFont برای لود کردن فونت از node_modules استفاده شد

2. **app/globals.css**
   - fallback فونت‌ها به فارسی تغییر یافت
   - ترتیب: Vazirmatn → Tahoma → Iranian Sans → sans-serif

3. **tailwind.config.ts**
   - fontFamily تنظیم شد تا از Vazirmatn استفاده کند
   - CSS variable: `--font-vazirmatn`

### 📦 پکیج نصب شده:

```bash
npm install vazirmatn
```

### 🚀 وضعیت:

- ✅ سرور در حال اجرا روی http://localhost:3002
- ✅ فونت Vazirmatn با موفقیت پیکربندی شد
- ✅ هیچ خطایی در کامپایل وجود ندارد

### 🎨 نحوه استفاده:

تمام متن‌های فارسی در اپلیکیشن به صورت خودکار با فونت Vazirmatn نمایش داده می‌شوند.

برای استفاده در کامپوننت‌ها از Tailwind classes استفاده کنید:

```tsx
<div className="font-sans">
  این متن با فونت وزیرمتن نمایش داده می‌شود
</div>
```

یا با استفاده از font-weight های مختلف:

```tsx
<p className="font-normal">عادی (400)</p>
<p className="font-medium">متوسط (500)</p>
<p className="font-semibold">نیمه‌بولد (600)</p>
<p className="font-bold">بولد (700)</p>
```
