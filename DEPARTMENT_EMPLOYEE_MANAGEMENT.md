# 🏢 راهنمای مدیریت بخش‌ها و کارمندان

## ✨ قابلیت‌های جدید اضافه شده

### 1️⃣ ویرایش و حذف بخش‌ها
### 2️⃣ مدیریت کارمندان بخش‌ها (قابل تگ در تسک‌ها)

---

## 🎯 مدیریت بخش‌ها (Departments)

### دسترسی:
- از Sidebar: **"مدیریت بخش‌ها"**
- از Dashboard: کارت **"مدیریت بخش‌ها"**
- URL: http://localhost:3002/departments

### امکانات:

#### ✅ ایجاد بخش جدید
- دکمه **"بخش جدید"**
- فیلدها:
  * نام بخش (الزامی)
  * توضیحات (اختیاری)
  * کلیدواژه‌ها (اختیاری) - برای روتینگ خودکار فیدبک‌ها

```
مثال:
نام: آشپزخانه
توضیحات: مدیریت امور آشپزخانه و غذا
کلیدواژه‌ها: آشپزخانه، غذا، نهار، ناهار، صبحانه، شام
```

#### ✏️ ویرایش بخش
- کلیک روی آیکون **مداد** در کارت بخش
- می‌توانید نام، توضیحات و کلیدواژه‌ها را تغییر دهید
- ADMIN و MANAGER می‌توانند ویرایش کنند

#### 🗑️ حذف بخش
- کلیک روی آیکون **سطل زباله** (فقط برای ADMIN)
- تایید نهایی درخواست می‌شود
- **محدودیت‌ها:**
  * اگر بخش دارای کاربر باشد، امکان حذف ندارد
  * اگر بخش دارای فیدبک باشد، امکان حذف ندارد
  * اگر بخش دارای تسک باشد، امکان حذف ندارد

### سطوح دسترسی:
- **ADMIN**: ایجاد، ویرایش، حذف همه بخش‌ها
- **MANAGER**: ایجاد، ویرایش بخش‌ها (نمی‌تواند حذف کند)
- **EMPLOYEE**: فقط مشاهده

---

## 👥 مدیریت کارمندان بخش‌ها (Employees)

### ⚠️ تفاوت مهم:
این بخش برای **کارمندان قابل تگ در تسک‌ها** است، نه کاربران سیستم!

- **Users** (مدیریت کاربران): کاربرانی که به سیستم لاگین می‌کنند
- **Employees** (کارمندان بخش‌ها): افرادی که در تسک‌ها تگ می‌شوند (ممکن است حساب کاربری نداشته باشند)

### دسترسی:
- از Sidebar: **"کارمندان بخش‌ها"**
- از Dashboard: کارت **"کارمندان بخش‌ها"**
- URL: http://localhost:3002/employees

### امکانات:

#### ✅ ایجاد کارمند جدید
- دکمه **"کارمند جدید"**
- فیلدها:
  * نام (الزامی) - مثال: "محمد رضایی"
  * سمت (اختیاری) - مثال: "تکنسین"، "مسئول آشپزخانه"
  * بخش (الزامی) - انتخاب از لیست بخش‌ها

```
مثال:
نام: علی احمدی
سمت: تکنسین برق
بخش: اداری
```

#### ✏️ ویرایش کارمند
- کلیک روی آیکون **مداد**
- می‌توانید نام، سمت و بخش را تغییر دهید
- MANAGER فقط می‌تواند کارمندان بخش خودش را ویرایش کند
- MANAGER نمی‌تواند کارمند را به بخش دیگری منتقل کند

#### 🗑️ حذف کارمند
- کلیک روی آیکون **سطل زباله** (فقط برای ADMIN)
- تایید نهایی درخواست می‌شود
- **محدودیت:**
  * اگر کارمند دارای تسک باشد، امکان حذف ندارد

#### 🔍 جستجو و فیلتر
- جستجو بر اساس نام
- فیلتر بر اساس بخش

### سطوح دسترسی:
- **ADMIN**:
  * مشاهده همه کارمندان (تمام بخش‌ها)
  * ایجاد کارمند در هر بخشی
  * ویرایش همه کارمندان
  * حذف همه کارمندان
  * انتقال کارمندان بین بخش‌ها

- **MANAGER**:
  * مشاهده کارمندان بخش خودش
  * ایجاد کارمند فقط در بخش خودش
  * ویرایش کارمندان بخش خودش
  * نمی‌تواند حذف کند
  * نمی‌تواند به بخش دیگر انتقال دهد

- **EMPLOYEE**: بدون دسترسی

---

## 🔄 فلوی کامل

### مثال: مدیریت بخش آشپزخانه

#### مرحله 1: ایجاد بخش
```
نام: آشپزخانه
توضیحات: مدیریت امور آشپزخانه و غذا
کلیدواژه‌ها: آشپزخانه، غذا، نهار، ناهار
```

#### مرحله 2: ایجاد کارمندان بخش
```
کارمند 1:
نام: سارا کریمی
سمت: سرآشپز
بخش: آشپزخانه

کارمند 2:
نام: احمد محمدی
سمت: کمک آشپز
بخش: آشپزخانه
```

#### مرحله 3: استفاده در تسک‌ها
- وقتی مدیر می‌خواهد تسک ایجاد کند
- می‌تواند از لیست کارمندان بخش، فرد مسئول را انتخاب کند
- مثلاً: تسک "تهیه غذای مهمانی" → محول به "سارا کریمی"

---

## 📱 API Endpoints

### Departments:

```
GET    /api/departments        # لیست بخش‌ها
POST   /api/departments        # ایجاد بخش جدید
GET    /api/departments/[id]   # مشاهده یک بخش
PATCH  /api/departments/[id]   # ویرایش بخش
DELETE /api/departments/[id]   # حذف بخش (فقط ADMIN)
```

### Employees:

```
GET    /api/employees           # لیست کارمندان
POST   /api/employees           # ایجاد کارمند جدید
GET    /api/employees/[id]      # مشاهده یک کارمند
PATCH  /api/employees/[id]      # ویرایش کارمند
DELETE /api/employees/[id]      # حذف کارمند (فقط ADMIN)
```

---

## 🎨 Sidebar نویگیشن

منوهای جدید اضافه شده:

### برای ADMIN و MANAGER:
- 👥 مدیریت کاربران (Users)
- 👷 **کارمندان بخش‌ها** (Employees) ⭐ جدید
- 🏢 مدیریت بخش‌ها (Departments)
- 📊 آمار و گزارش‌ها

### همچنین در Dashboard:
- کارت **"کارمندان بخش‌ها"** با آیکون UserCog

---

## 💡 نکات مهم

### 1. تفاوت Users و Employees:
```
Users (کاربران):
- دارای حساب کاربری (mobile + password)
- می‌توانند login کنند
- نقش دارند: ADMIN, MANAGER, EMPLOYEE
- مثال: "محمد رضایی - مدیر IT"

Employees (کارمندان بخش):
- ممکن است حساب کاربری نداشته باشند
- فقط برای تگ کردن در تسک‌ها
- سمت دارند: "تکنسین"، "آشپز"، "نگهبان"
- مثال: "علی احمدی - تکنسین برق"
```

### 2. کلیدواژه‌ها:
- با ویرگول (،) از هم جدا شوند
- برای روتینگ خودکار فیدبک‌ها استفاده می‌شوند
- مثال: `آشپزخانه، غذا، نهار، ناهار، صبحانه`

### 3. محدودیت‌های حذف:
- نمی‌توان بخش با کاربر/فیدبک/تسک حذف کرد
- نمی‌توان کارمند با تسک حذف کرد
- ابتدا باید وابستگی‌ها را حذف یا انتقال داد

### 4. دسترسی MANAGER:
- فقط بخش خودش
- نمی‌تواند حذف کند
- نمی‌تواند انتقال دهد به بخش دیگر

---

## 🚀 نحوه استفاده (مثال کامل)

### سناریو: راه‌اندازی بخش IT

#### 1. ایجاد بخش IT:
```
نام: فناوری اطلاعات
توضیحات: بخش IT و پشتیبانی فنی
کلیدواژه‌ها: کامپیوتر، سیستم، شبکه، اینترنت، نرم‌افزار، IT
```

#### 2. ایجاد کارمندان IT:
```
کارمند 1:
نام: رضا محمدی
سمت: تکنسین شبکه
بخش: فناوری اطلاعات

کارمند 2:
نام: مریم احمدی
سمت: برنامه‌نویس
بخش: فناوری اطلاعات
```

#### 3. ایجاد یک مدیر برای بخش IT:
- برو به **"مدیریت کاربران"** (Users)
- ایجاد کاربر جدید:
  ```
  نام: علی حسینی
  موبایل: 09121234567
  نقش: مدیر (MANAGER)
  بخش: فناوری اطلاعات
  رمز عبور: manager123
  ```

#### 4. حالا مدیر IT می‌تواند:
- فیدبک‌های بخش IT را ببیند
- تسک ایجاد کند و به "رضا محمدی" یا "مریم احمدی" محول کند
- کارمندان جدید به بخش اضافه کند

---

## 🔒 امنیت

- ✅ Role-based Access Control (RBAC)
- ✅ Department-scoped permissions for MANAGER
- ✅ Validation با Zod
- ✅ Session-based authentication
- ✅ Protection از حذف اشتباهی (check dependencies)

---

## ✅ تست سیستم

### تست 1: ایجاد و ویرایش بخش
1. به `/departments` بروید
2. بخش جدید بسازید
3. روی آیکون مداد کلیک کنید
4. کلیدواژه اضافه کنید
5. ذخیره کنید

### تست 2: ایجاد کارمند
1. به `/employees` بروید
2. کارمند جدید بسازید
3. سمت و بخش را مشخص کنید

### تست 3: تگ کردن در تسک
1. به `/tasks` بروید
2. تسک جدید بسازید
3. کارمند را از لیست انتخاب کنید

---

## 🎉 موفق باشید!

حالا می‌توانید بخش‌ها و کارمندان را به صورت کامل مدیریت کنید!
