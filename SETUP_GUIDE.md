# 📚 راهنمای نصب و راه‌اندازی سیستم فیدبک

## ✅ وضعیت فعلی

سیستم شما با موفقیت راه‌اندازی شده است! 🎉

- ✅ دیتابیس PostgreSQL لیارا متصل شد
- ✅ Schema به دیتابیس push شد
- ✅ کاربر Admin ایجاد شد
- ✅ سرور روی http://localhost:3002 در حال اجراست

## 🔑 اطلاعات ورود ادمین

```
موبایل: 09123456789
رمز عبور: admin123
کد OTP: 123456 (در محیط development)
```

## 🚀 دسترسی به برنامه

سرور در حال اجرا است روی:
**http://localhost:3002**

برای ورود:
1. به آدرس بالا بروید
2. شماره موبایل `09123456789` را وارد کنید
3. روی "ارسال کد تایید" کلیک کنید
4. کد `123456` را وارد کنید
5. رمز عبور `admin123` را وارد کنید

## 📋 مراحل بعدی

### 1. ایجاد بخش‌ها (Departments)

از منوی "مدیریت بخش‌ها" چند بخش ایجاد کنید. مثال:

```
نام: آشپزخانه
توضیحات: مدیریت امور آشپزخانه و غذا
کلیدواژه‌ها: آشپزخانه، غذا، نهار، صبحانه، ناهار، شام

نام: مالی
توضیحات: بخش مالی و حسابداری
کلیدواژه‌ها: مالی، حقوق، پرداخت، حساب، فیش

نام: IT
توضیحات: فناوری اطلاعات
کلیدواژه‌ها: کامپیوتر، سیستم، شبکه، اینترنت، نرم‌افزار

نام: اداری
توضیحات: امور اداری
کلیدواژه‌ها: اداری، مدارک، نامه، چراغ، برق، تعمیرات
```

**نکته مهم:** کلیدواژه‌ها برای روتینگ خودکار فیدبک‌ها استفاده می‌شوند.

### 2. ایجاد مدیران بخش (Managers)

از Prisma Studio یا با ایجاد API endpoint:

```typescript
// مثال: ایجاد یک مدیر
const manager = await prisma.user.create({
  data: {
    mobile: "09121111111",
    name: "علی احمدی",
    password: await bcrypt.hash("password123", 10),
    role: "MANAGER",
    departmentId: "department-id-here",
  },
});
```

### 3. ایجاد کارمندان قابل تگ (Employees)

از صفحه `/employees` (در حال ساخت UI) یا از API:

```bash
curl -X POST http://localhost:3002/api/employees \
  -H "Content-Type: application/json" \
  -d '{
    "name": "محمد رضایی",
    "position": "تکنسین",
    "departmentId": "department-id"
  }'
```

### 4. ایجاد کاربران کارمند (Employee Users)

برای کارمندانی که می‌خواهند فیدبک ارسال کنند:

```typescript
const employee = await prisma.user.create({
  data: {
    mobile: "09122222222",
    name: "سارا کریمی",
    password: await bcrypt.hash("password123", 10),
    role: "EMPLOYEE",
    departmentId: "department-id",
  },
});
```

## 🔄 فلوی کامل سیستم

### مثال عملی:

1. **کارمند** وارد سیستم می‌شود و فیدبک می‌فرستد:
   ```
   عنوان: چراغ آشپزخانه خراب است
   محتوا: چراغ سمت راست آشپزخانه روشن نمی‌شود
   نوع: انتقادی
   بخش: اداری
   ```

2. **سیستم** کلمه "آشپزخانه" را تشخیص می‌دهد و:
   - اگر بخشی با کلیدواژه "آشپزخانه" داشته باشد، به آن بخش روت می‌کند
   - در غیر این صورت به بخش انتخاب شده توسط کاربر ارسال می‌شود

3. **مدیر بخش** فیدبک را می‌بیند و:
   - تبدیل به تسک می‌کند
   - فرد مسئول (Employee) را تگ می‌کند
   - اولویت را مشخص می‌کند (HIGH, MEDIUM, LOW)

4. **فرد مسئول** تسک را می‌بیند و:
   - وضعیت را به IN_PROGRESS تغییر می‌دهد
   - کار را انجام می‌دهد
   - وضعیت را به COMPLETED تغییر می‌دهد
   - تیک "نمایش در بورد عمومی" را می‌زند

5. **همه کارمندان** می‌توانند:
   - در صفحه "بورد افتخارات" این تسک را ببینند
   - متوجه شوند که این کار انجام شده

## 🎯 امکانات کلیدی

### برای کارمندان (EMPLOYEE):
- ✅ ارسال فیدبک (انتقادی / پیشنهادی / نظرسنجی)
- ✅ ارسال فیدبک ناشناس
- ✅ مشاهده فیدبک‌های خودشان
- ✅ مشاهده تسک‌های محول شده
- ✅ مشاهده اعلانات
- ✅ مشاهده بورد افتخارات

### برای مدیران (MANAGER):
- ✅ همه امکانات کارمند +
- ✅ مشاهده همه فیدبک‌های بخش
- ✅ تبدیل فیدبک به تسک
- ✅ تگ کردن افراد مسئول
- ✅ فوروارد تسک به بخش دیگر
- ✅ ارسال اعلان برای بخش
- ✅ مدیریت کارمندان بخش

### برای مدیرعامل (ADMIN):
- ✅ همه امکانات +
- ✅ مشاهده همه فیدبک‌ها و تسک‌ها
- ✅ مدیریت بخش‌ها
- ✅ ارسال اعلان عمومی
- ✅ دسترسی کامل به آمار
- ✅ مشاهده فیدبک‌های ناشناس با نام واقعی

## 🗂️ ساختار صفحات

```
/ - داشبورد اصلی
/login - ورود با موبایل و OTP
/feedback - لیست فیدبک‌ها
/feedback/new - ارسال فیدبک جدید
/tasks - لیست و مدیریت تسک‌ها
/announcements - مشاهده اعلانات
/public-board - بورد افتخارات (تسک‌های تکمیل شده)
/departments - مدیریت بخش‌ها (ADMIN/MANAGER)
/analytics - آمار و نمودارها (ADMIN/MANAGER)
```

## 🔧 دستورات مفید

```bash
# اجرای سرور development
npm run dev

# Build برای production
npm run build
npm start

# دسترسی به Prisma Studio (مدیریت دیتابیس)
npm run db:studio

# Push تغییرات schema به دیتابیس
npm run db:push

# ایجاد کاربر ادمین جدید
ADMIN_MOBILE="09111111111" ADMIN_PASSWORD="newpass" npm run create-admin
```

## 🌐 دیپلوی روی لیارا

### مرحله 1: نصب Liara CLI

```bash
npm i -g @liara/cli
```

### مرحله 2: Login

```bash
liara login
```

### مرحله 3: ایجاد اپلیکیشن

در پنل لیارا یک اپلیکیشن Next.js ایجاد کنید.

### مرحله 4: تنظیم Environment Variables

در تنظیمات اپلیکیشن، این متغیرها را اضافه کنید:

```env
DATABASE_URL=postgresql://root:54EIo4e6un4txsQzllqNyrMK@vinson.liara.cloud:33591/postgres?schema=public
NEXTAUTH_URL=https://your-app.liara.run
NEXTAUTH_SECRET=generate-a-secure-random-string-here
NODE_ENV=production
OTP_ENABLED=true
OTP_DEFAULT=123456
```

**نکته:** برای NEXTAUTH_SECRET از این دستور استفاده کنید:
```bash
openssl rand -base64 32
```

### مرحله 5: Deploy

```bash
liara deploy
```

### مرحله 6: اجرای Commands در لیارا

بعد از deploy، در Terminal لیارا:

```bash
npm run db:push
npm run create-admin
```

## 📱 تبدیل به PWA

برنامه آماده برای نصب به عنوان PWA است. فقط کافیست:

1. در مرورگر Chrome روی آیکون نصب کلیک کنید
2. یا از منوی "Install App" استفاده کنید

## 🔐 امنیت

- ✅ احراز هویت با NextAuth.js
- ✅ Hash کردن رمز عبور با bcrypt (10 rounds)
- ✅ JWT Session Strategy
- ✅ Role-based Access Control
- ✅ Validation با Zod
- ✅ HTTPS برای production (لیارا به صورت خودکار)

## 📞 پشتیبانی

اگر مشکلی پیش آمد:

1. لاگ‌های سرور را چک کنید
2. دیتابیس را با `npm run db:studio` بررسی کنید
3. Environment Variables را مجدداً چک کنید

## 🎨 سفارشی‌سازی

### تغییر رنگ‌ها:

فایل `tailwind.config.ts` را ویرایش کنید.

### تغییر لوگو:

فایل‌های PWA manifest را در `public/` ویرایش کنید.

### اضافه کردن صفحه جدید:

در پوشه `app/` فولدر جدید بسازید.

---

**موفق باشید! 🚀**
